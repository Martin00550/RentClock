import { Inngest } from "inngest";

// Create a client to send and receive events
export const inngest = new Inngest({ id: "rent-clock" });

// Define your functions here
// Example: export const myInngestFunction = inngest.createFunction(...)
